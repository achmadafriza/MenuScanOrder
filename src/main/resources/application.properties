server.port=${PORT:8080}
spring.application.name=menu-scan-order

service.storage.url=${STATIC_FILE_STORE_URL:file:///C:/Programming/INFS7202/project/img}
service.storage.root=${STATIC_FILE_STORE_DIR:./img}
service.storage.rolling.dir=/rolling
service.storage.static.dir=/static

spring.jackson.mapper.accept-case-insensitive-enums = true

spring.datasource.url=jdbc:mysql://${MYSQL_HOST:127.0.0.1}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:infs7202}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Australia/Brisbane&useLegacyDatetimeCode=false
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:admin}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect.storage_engine=innodb

# logs the SQL statements
#logging.level.org.hibernate.sql = debug
# Logs the JDBC parameters passed to a query
#logging.level.org.hibernate.type = trace